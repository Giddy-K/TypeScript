let user = {name: "Gideon", age: 24}//ignore the error which appered after generating the .js file.

console.log("Gideon");
console.log(user.name);

//commands in terminal
// 1. tsc intro.ts to generate the .js file